>>> import json
>>> from stellarsApp import Post

with open('db/posts.json') as f:
    posts_json=json.load(f)

for post in posts_json:
    post = Post(title=post['title'], content=post['content'], author_id=post['user_id'], movie_id=post['movie_id'])
    post.save()